# Database Connection
# Replace with your MongoDB connection string
DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/universe-hub"

# NextAuth Configuration
# Generate a secret with `openssl rand -base64 32` or just a long random string
NEXTAUTH_SECRET="change-this-to-a-secure-random-secret"
NEXTAUTH_URL="http://localhost:3000"


# External APIs
# NASA API Key (Get one at https://api.nasa.gov/)
NEXT_PUBLIC_NASA_API_KEY="DEMO_KEY"

# Node Environment
NODE_ENV="development"
